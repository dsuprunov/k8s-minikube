name: Deploy Staging

on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  deploy-staging:
    runs-on: ubuntu-latest
    env:
      XGITHUB_ENV_VAR: ${{ vars.XGITHUB_ENV_SECRET }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

#      - name: Load environment variables from env-vars.yml
#        run: |
#          echo "Loading environment variables for staging"
#          #eval $(cat infra/demo-portal/overlays/dev/env-vars.yml | sed 's/: /=/g')
#
#      - name: Load secrets from env-secrets.yml
#        run: |
#          echo "Loading secrets for staging"
#          eval $(cat infra/demo-portal/overlays/dev/env-secrets.yml | sed 's/: /=/g')

      - name: Print env
        run: |
          env | sort
