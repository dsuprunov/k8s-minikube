resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: demo-portal-configmap
    files:
      - .configmap.env

secretGenerator:
  - name: demo-portal-secret
    files:
      - .secret.env